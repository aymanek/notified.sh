---
description: Show pairing status and API reachability
---

Run this Bash command for the user:

```
node "${CLAUDE_PLUGIN_ROOT}/dist/cli.cjs" status
```

This shows whether the user is paired, the bot username, the API reachability check, and the hook install status. Report the output verbatim to the user.
