// Replaced at build time by tsup's `define` from packages/cli/package.json,
// which release-please keeps in sync with the released tag.
export const PKG_VERSION = process.env.NOTIFIED_PKG_VERSION ?? "0.0.0-dev";
