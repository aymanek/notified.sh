{
  "name": "notified.sh",
  "version": "0.1.7",
  "description": "Telegram notifications for Claude Code limit resets.",
  "type": "module",
  "main": "./dist/cli.cjs",
  "bin": {
    "notified": "./dist/cli.cjs"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "qrcode-terminal": "^0.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@notified.sh/shared": "workspace:*",
    "@types/qrcode-terminal": "^0.12.2",
    "tsup": "^8.4.0",
    "vitest": "^2.1.0"
  }
}
