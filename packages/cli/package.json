{
  "name": "notified.sh",
  "version": "0.0.0",
  "private": true,
  "description": "Telegram notifications for Claude Code limit resets.",
  "type": "module",
  "main": "./dist/cli.js",
  "bin": {
    "notified": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@notified.sh/shared": "workspace:*"
  }
}
